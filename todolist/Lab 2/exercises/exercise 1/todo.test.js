import Todo from '../../../app/todo';
import React from 'react';
import { mount } from 'enzyme';

test('TodoComponent calls doneChange when todo is clicked', () => {
	/* To be completed
	
	You should define necessary constants to render the Todo component. 
	Also find a way to simulate the "onclick" event.

	The matcher should be of this format: expect(...).toBeCalledWith(...);
	*/
    
});
