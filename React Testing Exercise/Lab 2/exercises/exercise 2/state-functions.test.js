import { toggleDone, deleteTodo } from '../../../app/state-functions';

test('tooggleDone completes an incomplete todo', () => {
  /* To be completed

  The matcher should be of this format: expect(...).toEqual(...);
  */
});

test('deleteTodo deletes the todo it is given', () => {
  /* To be completed

  The matcher should be of this format: expect(...).toEqual(...);
  */
});
